#3.3 - Gleichheit Arrays

Eine Funktion f(int[],int[]), die zwei eindimensionale int-Arrays als Parameter übergeben bekommt und überprüft, ob die darin gespeicherten Werte jeweils gleich sind

Beispiel: f([1,4,3], [1, 4, 3]) == true